{
  "name": "jev-ego",
  "version": "0.1.0",
  "description": "A browser agent with a dynamic, indexed action space, running inside ego lite.",
  "type": "module",
  "license": "MIT",
  "bin": {
    "jev-ego": "./bin/jev-ego.mjs"
  },
  "scripts": {
    "build": "node scripts/build.mjs",
    "test": "vitest run",
    "lint": "tsc --noEmit"
  },
  "engines": {
    "node": ">=20"
  },
  "packageManager": "pnpm@10.14.0",
  "pnpm": {
    "onlyBuiltDependencies": ["esbuild"]
  },
  "devDependencies": {
    "@types/node": "^24.5.2",
    "esbuild": "^0.25.9",
    "typescript": "^5.9.2",
    "vitest": "^3.2.4"
  }
}
